version: "3.9"

services:
  crm-agent:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # persist the SQLite database across restarts
    restart: unless-stopped
